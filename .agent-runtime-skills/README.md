# Agent Runtime Skills

This directory is dedicated to runtime-loaded chat agent skills.

- Put one skill per subfolder.
- Each skill folder must include a `SKILL.md` file with frontmatter fields:
  - `name`
  - `description`

Example:

```
.agent-runtime-skills/
|__my-skill/
   |__references/
   |__SKILL.md
```
